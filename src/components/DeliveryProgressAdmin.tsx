import { useEffect, useState } from "react";
import { Save, Truck } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Switch } from "@/components/ui/switch";
import { Textarea } from "@/components/ui/textarea";
import { useToast } from "@/hooks/use-toast";

type Setting = { network: string; enabled: boolean; is_default?: boolean; source: "manual" | "orders" | "fake"; min_minutes: number | string; max_minutes: number | string; rotation_minutes: number | string; fake_enabled: boolean; fake_prefix: string; fake_count: number | string; status_color: "green" | "red" | "yellow"; message: string; auto_enabled: boolean; auto_min_minutes: number | string; auto_max_minutes: number | string };
const networks = ["mtn", "mtn_express", "telecel", "airteltigo"];
const labels: Record<string, string> = { mtn: "MTN", mtn_express: "MTN Express", telecel: "Telecel", airteltigo: "AirtelTigo" };
const prefixes: Record<string, string> = { mtn: "024, 054, 055, 059", mtn_express: "024, 054, 055, 059", telecel: "020, 050", airteltigo: "026, 056" };
const defaults = networks.map((network) => ({ network, enabled: true, source: "orders" as const, min_minutes: 30, max_minutes: 240, rotation_minutes: 30, fake_enabled: false, fake_prefix: prefixes[network], fake_count: 10, status_color: "green" as const, message: `${labels[network]} orders are being processed. Please allow the estimated delivery window.`, auto_enabled: false, auto_min_minutes: 30, auto_max_minutes: 240 }));

export default function DeliveryProgressAdmin() {
  const { toast } = useToast();
  const [settings, setSettings] = useState<Setting[]>([]);
  const [globalEnabled, setGlobalEnabled] = useState(true);
  const [defaultNetwork, setDefaultNetwork] = useState("mtn");
  const [saving, setSaving] = useState(false);
  useEffect(() => { supabase.from("delivery_progress_settings").select("network, enabled, is_default, source, min_minutes, max_minutes, rotation_minutes, fake_enabled, fake_prefix, fake_count, status_color, message, auto_enabled, auto_min_minutes, auto_max_minutes").order("network").then(({ data }) => { const rows = data ?? []; setGlobalEnabled(rows.find((s: any) => s.network === "__global__")?.enabled ?? true); setDefaultNetwork(rows.find((s: any) => s.is_default)?.network ?? "mtn"); setSettings(networks.map((n) => ({ ...defaults.find((d) => d.network === n)!, ...(rows.find((s: any) => s.network === n) ?? {}) }))); }); }, []);
  const update = (network: string, patch: Partial<Setting>) => setSettings((items) => items.map((item) => item.network === network ? { ...item, ...patch } : item));
  const save = async () => { setSaving(true); const normalized = settings.map((s) => { const min = Math.max(2, Number(s.min_minutes) || 2); const max = Math.max(min, Number(s.max_minutes) || min); const autoMin = Math.max(0, Number(s.auto_min_minutes) || 0); const autoMax = Math.max(autoMin, Number(s.auto_max_minutes) || autoMin); return { ...s, min_minutes: min, max_minutes: max, rotation_minutes: Math.max(1, Number(s.rotation_minutes) || 1), fake_count: Math.min(100, Math.max(1, Number(s.fake_count) || 1)), auto_min_minutes: autoMin, auto_max_minutes: autoMax, fake_prefix: s.fake_prefix.split(",").map((p) => p.replace(/\D/g, "").slice(0, 3)).filter(Boolean).join(", "), source: s.fake_enabled ? "fake" : s.source === "fake" ? "orders" : s.source, is_default: s.network === defaultNetwork, updated_at: new Date().toISOString() }; }); const { error } = await supabase.rpc("save_delivery_progress_settings", { payload: [...normalized, { network: "__global__", enabled: globalEnabled, is_default: false, source: "manual", min_minutes: 2, max_minutes: 2, rotation_minutes: 1, fake_enabled: false, fake_prefix: "", fake_count: 1, status_color: "green", message: "", auto_enabled: false, auto_min_minutes: 0, auto_max_minutes: 0, updated_at: new Date().toISOString() }] }); setSaving(false); toast(error ? { title: "Could not save delivery settings", description: "Apply the delivery progress SQL migration first.", variant: "destructive" } : { title: "Delivery settings saved" }); };
  return <Card><CardHeader><CardTitle className="flex items-center gap-2"><Truck className="size-5" />Delivery Progress Controls</CardTitle></CardHeader><CardContent className="flex flex-col gap-6"><div className="flex items-center justify-between rounded-lg border p-4"><div><Label>Show delivery progress card</Label><p className="text-sm text-muted-foreground">Turn the customer-facing card on or off.</p></div><Switch checked={globalEnabled} onCheckedChange={setGlobalEnabled} /></div>{settings.map((s) => <section key={s.network} className="flex flex-col gap-4 rounded-xl border p-4"><div className="flex items-center justify-between"><div><h3 className="font-semibold">{labels[s.network]}</h3><p className="text-sm text-muted-foreground">Network-specific customer progress and testing controls.</p></div><Switch checked={s.enabled} onCheckedChange={(checked) => update(s.network, { enabled: checked })} /></div><div className="grid gap-4 sm:grid-cols-2"><div className="flex flex-col gap-2"><Label>Use fake delivery data</Label><Switch checked={s.fake_enabled} onCheckedChange={(checked) => update(s.network, { fake_enabled: checked, source: checked ? "fake" : "orders" })} /></div><div className="flex flex-col gap-2"><Label>Default network</Label><Switch checked={defaultNetwork === s.network} onCheckedChange={(checked) => checked && setDefaultNetwork(s.network)} /></div><div className="flex flex-col gap-2"><Label>Estimate from minutes</Label><Input type="number" min={2} value={s.min_minutes} onChange={(e) => update(s.network, { min_minutes: e.target.value })} /></div><div className="flex flex-col gap-2"><Label>Estimate to minutes</Label><Input type="number" min={2} value={s.max_minutes} onChange={(e) => update(s.network, { max_minutes: e.target.value })} /></div><div className="flex flex-col gap-2"><Label>Fake number rotation minutes</Label><Input type="number" min={1} value={s.rotation_minutes} onChange={(e) => update(s.network, { rotation_minutes: e.target.value })} /></div><div className="flex flex-col gap-2"><Label>Fake number count</Label><Input type="number" min={1} max={100} value={s.fake_count} onChange={(e) => update(s.network, { fake_count: e.target.value })} /></div><div className="flex flex-col gap-2 sm:col-span-2"><Label>Network prefixes</Label><Input value={s.fake_prefix} placeholder={prefixes[s.network]} onChange={(e) => update(s.network, { fake_prefix: e.target.value })} /><p className="text-xs text-muted-foreground">Comma-separated 3-digit prefixes. Example: {prefixes[s.network]}</p></div><div className="flex flex-col gap-2 sm:col-span-2"><Label>Customer-facing message</Label><Textarea value={s.message} placeholder={`${labels[s.network]} orders are being processed. Please allow the estimated delivery window.`} onChange={(e) => update(s.network, { message: e.target.value })} /></div><div className="flex items-center justify-between rounded-lg border p-3 sm:col-span-2"><div><Label>Delivery automation</Label><p className="text-xs text-muted-foreground">Automatically marks processing orders Delivered within this network&apos;s range.</p></div><Switch checked={s.auto_enabled} onCheckedChange={(checked) => update(s.network, { auto_enabled: checked })} /></div><div className="flex flex-col gap-2"><Label>Automation from minutes</Label><Input type="number" min={0} value={s.auto_min_minutes} onChange={(e) => update(s.network, { auto_min_minutes: e.target.value })} /></div><div className="flex flex-col gap-2"><Label>Automation to minutes</Label><Input type="number" min={0} value={s.auto_max_minutes} onChange={(e) => update(s.network, { auto_max_minutes: e.target.value })} /></div></div></section>)}<Button onClick={save} disabled={saving} className="self-start"><Save className="mr-2 size-4" />{saving ? "Saving..." : "Save delivery settings"}</Button></CardContent></Card>;
}
